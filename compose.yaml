services:
  backend:
    depends_on:
      database:
        condition: service_healthy

  database:
    image: postgres:18
    volumes:
      - database:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  database:
